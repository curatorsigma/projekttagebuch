//! (Re)sync
//! (a) Users from LDAP into the DB
//! (b) Project Membership from the DB into Matrix

/// Resync all users from LDAP into the DB.
async fn do_user_resync() {}

async fn get_users_from_ldap() {}

/// Resync all Project Memberships from the DB into Matrix
async fn do_project_membership_resync() {}
